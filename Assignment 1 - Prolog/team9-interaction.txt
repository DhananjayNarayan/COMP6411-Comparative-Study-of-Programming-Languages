## QUERIES: 

4.2.1
What classes, if any, are present in the program? - NON-GROUND
Query - class(X).

4.2.2
What methods, if any, does class ’Human’ define? - NON-GROUND
Query - defines(human,X,method,_).

4.2.3 - 
Does class ’Android’ define an attribute named ’version’? - GROUND
Query - defines(android,version,attribute,_).

4.2.4
Does class ’Machine’ define a method named ’hasMemories()’? - GROUND
Query - defines(machine,hasmemories,method,_).

4.2.5
Which class, if any, extends class ’Human’? - NON-GROUND
Query - extends(X,human).

4.2.6
Which type, if any, implements interface ’Behavior2’? - NON-GROUND
Query - implements(X,behavior2).

INTERACTION WITH SYSTEM:

?- class(X).
X = human ;
X = bladerunner ;
X = android ;
X = machine ;
X = android2.

?- defines(human,X,method,_).
X = act ;
X = reason ;
X = hasempathy.

?- defines(android,version,attribute,_).
true.

?- defines(machine,hasmemories,method,_).
false.

?- extends(X,human).
X = bladerunner.

?- implements(X,behavior2).
X = android2.



Note - Doubt to be discussed in meeting. 
What implements behavior2? Android or android2>
